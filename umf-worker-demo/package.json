{
  "name": "module-federation-dynamic-webworker-imports",
  "version": "0.0.0",
  "author": "Jason Thorpe",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run --parallel build",
    "start": "lerna run --parallel start",
    "serve": "lerna run --parallel serve"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
